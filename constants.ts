
import { Book, Level, WordCount } from './types';

export const BOOKS: Book[] = [Book.JaneEyre, Book.MonteCristo];
export const LEVELS: Level[] = [Level.Beginner, Level.Intermediate, Level.Advanced];
export const WORD_COUNTS: WordCount[] = [10, 20, 30];
